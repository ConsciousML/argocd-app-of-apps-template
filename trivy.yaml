scan:
  skip-dirs:
    - "**/.terragrunt-stack"
    - "**/.terragrunt-cache"
  skip-files:
    - "**/.env"

misconfiguration:
  scanners:
    - kubernetes
    - helm

severity:
  - HIGH
  - CRITICAL

exit-code: 1
