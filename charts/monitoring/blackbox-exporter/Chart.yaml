apiVersion: v2
name: blackbox-exporter
description: Blackbox exporter, HTTP reachability probing for the cluster's private and public endpoints
type: application
version: 0.1.0
dependencies:
  - name: prometheus-blackbox-exporter
    version: 11.16.0
    repository: https://prometheus-community.github.io/helm-charts
