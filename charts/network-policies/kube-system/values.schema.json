{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Values",
  "type": "object",
  "required": ["vpcEndpointCidrs"],
  "properties": {
    "vpcEndpointCidrs": {
      "type": "object",
      "required": ["ec2", "sqs", "iam"],
      "properties": {
        "ec2": { "type": "array", "minItems": 1, "items": { "type": "string" } },
        "sqs": { "type": "array", "minItems": 1, "items": { "type": "string" } },
        "iam": { "type": "array", "minItems": 1, "items": { "type": "string" } }
      }
    }
  }
}
