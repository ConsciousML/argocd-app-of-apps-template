# Used only by scripts/validate-helm.sh so standalone `helm template` and
# kubeconform pass. Real values are injected by the catalog
# (terragrunt-template-catalog-eks) via appParams, from the EKS cluster name and
# units/eks/addons/karpenter/iam's interruption queue. See
# units/eks/addons/argocd/app_of_apps in that repo.
karpenter:
  settings:
    clusterName: "placeholder-cluster"
    interruptionQueue: "placeholder-queue"
