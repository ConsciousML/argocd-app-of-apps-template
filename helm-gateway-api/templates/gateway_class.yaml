apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ .Values.gatewayClassName | quote }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  controllerName: gateway.k8s.aws/alb
